/*
 * Simple String allocation and addition test
 */
class tests.Maths
{
    function main() static
    {
var i;
for (i = 0; i < 10; i++)
{
var r = system.lang.Maths.randomInt(1, 10);
system.io.File.out.writeln("randomInt: r=" + r);
}
    }
}

